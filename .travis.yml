language: python
python: 3.6
before_install:
  - sudo apt-get install octave
install:
  - pip install -r requirements.txt
  - pip install oct2py codecov pytest pytest-cov
script:
  - pytest -ss -q tests/test_cascade.py
after_success:
  - codecov